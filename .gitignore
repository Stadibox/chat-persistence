# dependencies
node_modules
.pnpm-store

# build outputs
dist
build
.next
.turbo
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# cache
.cache
.parcel-cache
.eslintcache
.prettiercache
.cache/prompt-cache

# editor / OS
.DS_Store
Thumbs.db
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# local artifacts
*.local
.runs/
runs/
agents-snapshots/

# corpus Stadibox — local-only por regla del CEO (ULTRAPLAN §7 R4).
# El contenido es sensible (credenciales plaintext flagged, role-permission-matrix,
# business-rules). Lo sincronizamos a docs/ via scripts/sync-corpus.ts y NO se commitea.
docs/*
!docs/.gitkeep
!docs/README.md

# supabase
supabase/.branches
supabase/.temp

# Claude Code local settings (permisos per-máquina, no compartir)
.claude/settings.local.json
